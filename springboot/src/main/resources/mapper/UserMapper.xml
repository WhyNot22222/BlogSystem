<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yn.mapper.UserMapper">

    <select id="selectAll" resultType="com.yn.entity.User">
        select * from user order by id
    </select>

    <select id="findByUsername" resultType="com.yn.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findByEmail" resultType="com.yn.entity.User">
        SELECT * FROM user WHERE email = #{email}
    </select>

    <select id="findById" resultType="com.yn.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <insert id="insertUser" parameterType="com.yn.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, email, password) 
        VALUES (#{username}, #{email}, #{password})
        ON DUPLICATE KEY UPDATE 
            username = VALUES(username) <!-- 触发唯一约束异常 -->
    </insert>

    <update id="updateUser" parameterType="com.yn.entity.User">
        UPDATE user
        SET username = #{username},
            avatar = #{avatar},
            bio = #{bio},
            phone = #{phone}
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE user
        SET password = #{newPassword}
        WHERE id = #{userId}
    </update>

    <update id="updateEmail">
        UPDATE user
        SET email = #{newEmail}
        WHERE id = #{userId}
    </update>

</mapper>