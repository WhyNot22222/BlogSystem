<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yn.mapper.TagMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tags(name)
        VALUES(#{name})
    </insert>

    <delete id="deleteById">
        DELETE FROM tags WHERE id = #{id}
    </delete>

    <select id="selectAll" resultType="com.yn.entity.Tag">
        SELECT id, name FROM tags
    </select>

    <select id="selectByName" resultType="com.yn.entity.Tag">
        SELECT id, name FROM tags WHERE name = #{name}
    </select>

    <select id="selectById" resultType="com.yn.entity.Tag">
        SELECT id, name FROM tags WHERE id = #{id}
    </select>

    <select id="selectHotTags" resultType="com.yn.entity.Tag">
        SELECT t.id, t.name, COUNT(pt.post_id) as usage_count 
        FROM tags t
        LEFT JOIN post_tags pt ON t.id = pt.tag_id
        GROUP BY t.id
        ORDER BY usage_count DESC
        LIMIT 10
    </select>

</mapper>